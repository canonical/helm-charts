RabbitMQ has been deployed.

{{- if eq .Values.service.type "ClusterIP" }}
Port-forward the Service and connect to amqp://localhost:{{ .Values.service.port }}:
  kubectl port-forward svc/{{ include "rabbitmq.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
{{- else }}
Connect to the Service {{ include "rabbitmq.fullname" . }} on port {{ .Values.service.port }} (type: {{ .Values.service.type }}).
{{- end }}
